<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="hello.hellospring.mapper.CommentMapper">
    <insert id="insertComment" parameterType="hello.hellospring.vo.CommentReqVo">
        insert into comment(content, date, userId, title, projectNm)
        values(#{content}, #{date}, #{userId}, #{title}, #{projectNm});
    </insert>
</mapper>